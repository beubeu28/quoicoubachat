<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}
        <link rel="stylesheets" href="{{asset('build/app.css')}}"/>

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    
    <body>
    <header class="bg-white text-rose-300 border-rose-300 border-b-2 p-4">
        <div class="container mx-auto flex justify-between items-center">
            <!-- Logo -->
            <div class="flex items-center">
                <img src="images\logo.png" alt="Logo" class="w-10 h-10 mr-2">
                <span class="text-lg font-bold">MonSite</span>
            </div>

            <!-- Liens de navigation -->
            <nav class="space-x-4">
                <a href="#" class="hover:text-rose-300">Accueil</a>
                <a href="#" class="hover:text-rose-300">Recherche</a>
                <a href="#" class="hover:text-rose-300">Utilisateur</a>
                <a href="#" class="hover:text-rose-300">Panier</a>
            </nav>
        </div>
    </header>
        {% block body %}{% endblock %}

    
    </body>
</html>
