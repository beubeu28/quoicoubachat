{% extends 'base.html.twig' %}

{% block title %}Messagerie{% endblock %}

{% block body %}
    <h1>Messagerie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ messagerie.id }}</td>
            </tr>
            <tr>
                <th>Motif</th>
                <td>{{ messagerie.motif }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ messagerie.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_messagerie_index') }}">back to list</a>

    <a href="{{ path('app_messagerie_edit', {'id': messagerie.id}) }}">edit</a>

    {{ include('messagerie/_delete_form.html.twig') }}
{% endblock %}
